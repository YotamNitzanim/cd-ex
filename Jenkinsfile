pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'ls -lah'
            }
        }
        stage('push') {
            steps {
                sh 'ls'
            }
        }
        
    }
}
