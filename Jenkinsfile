pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'ls -lah'
            }
        }
        stage('push') {
            steps {
                sh 'history'
            }
        }
        stage('deploy') {
            steps {
                sh 'python --version'
            }
        }
    }
}
