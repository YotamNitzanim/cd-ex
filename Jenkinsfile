pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'ls -lah'
            }
        }
        stage('push') {
            steps {
//                 sh 'some command...'
            }
        }
        stage('deploy') {
            steps {
                sh 'python --version'
            }
        }
    }
}
