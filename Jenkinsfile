pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'ls -lah'
                sh 'ls'
            }
        }
        stage('test') {
            steps {
                sh 'ls -lah'
            }
        }
        
        
    }
}
